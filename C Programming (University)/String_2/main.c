#include <stdio.h>
#include <stdlib.h>

int main()
{
    char A[30];
    gets(A); /*s for satring, get+s=gets, for taking string we can use "gets"*/
    printf("Input Statement: %s", A);

    return 0;
}
