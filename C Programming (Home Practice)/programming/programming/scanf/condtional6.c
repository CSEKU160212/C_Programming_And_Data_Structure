#include <stdio.h>
int main()
{
    int number, remainder;
    number=5;
    remainder = number % 2;
    if(number== 0) {    printf("the number is even\n");}
    else {    printf("the number is even\n");}
    return 0;
}
